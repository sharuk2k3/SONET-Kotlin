package com.example.myapplication

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import android.view.View
import android.widget.EditText
import android.widget.Toast

class MainActivity2 : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_main2)

        //var name = findViewById<EditText>(R.id.et_2_1)
       // var gender = findViewById<EditText>(R.id.et_2_2)
        //var age = findViewById<EditText>(R.id.et_2_3)
       // var btn = findViewById<EditText>(R.id.btn_2)

        //btn.setOnClickListener(View.OnClickListener {
           // val person = Person()
            ///person.name = name.text.toString()
            //person.gender = gender.text.toString()
           // person.age = age.text.toString().toInt()

            //var result = if(person.age!=0) "${person.name}" else "Person is Minor")
            //var result = if(person.age>=18) "you can enter ${person.name}" else "you cannot enter ${person.name}"
           // Toast.makeText(this, result, Toast.LENGTH_LONG).show()
      //  });
    }
}